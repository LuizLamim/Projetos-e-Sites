import numpy as np

# Criando os vetores como arrays do NumPy
vetor_a = np.array([1, 5, 9])
vetor_b = np.array([2, 3, 4])

# O NumPy permite usar o sinal de "+" diretamente para somar os elementos
resultado = vetor_a + vetor_b

print(f"Resultado da soma (NumPy): {resultado}") # Saída: [ 3  8 13]